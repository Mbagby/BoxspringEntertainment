#content.container.uui
  .grid.rows
    .category-slick
      - @categories.each do |category|
        %div
          = link_to category_path(category) do
            %h1#formatHeader= category.title
          = link_to category_path(category) do
            .banner{:id => "#{category.snap_shot}"}
  -# .rows
  -#   - @categories.each do |category|
  -#     .row
  -#       = link_to category_path(category) do
  -#         %h1#formatHeader= category.title
  -#       = link_to category_path(category) do
  -#         .banner{:id => "#{category.snap_shot}"}


:javascript
  $(document).ready(function(){
    $('.category-slick').slick({
      dots:true,
      infinite: false,
      slidesToShow:3,
      slidesToScroll: 1
    });
  });
