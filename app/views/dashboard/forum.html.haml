.dashboard-page
  .col-sm-3.side_nav
    = render "left_side_dashboard"
  .col-sm-9
    .row
      .col-sm-12.content-header
        %label alerts - messages - ad space
    .row.forum-content
      .col-sm-12
        %h1.forum-title Message Boards
      - Category.all.each do |cat|
        .col-sm-8.forum-item
          = link_to message_board_category_path(cat) do
            %h3= cat.title
        .col-sm-4.forum-item
          %label
            = cat.messages.count
            comments
